<h1>Products</h1>

<ul>
<% @products.each do |product| %>
  <li>
    <div class="product-wrapper">
      <a href="<%= product_path(product) %>"><%= product.name %></a><span>: $<%= product.formatted_price %></span>
    </div> 
  </li>
<% end %>
</ul> 
<%= link_to "New Product", new_product_path %>

